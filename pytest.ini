[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -m "not integration"
markers =
    integration: sobe o app completo em subprocess (lento; requer deps e opcionalmente webcam)
